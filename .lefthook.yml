# Lefthook configuration for snap-css
# https://lefthook.dev/configuration/

min_version: 1.0.0

pre-commit:
  commands:
    tidy:
      run: go mod tidy && git diff --exit-code go.mod go.sum
    lint:
      run: golangci-lint run ./...
